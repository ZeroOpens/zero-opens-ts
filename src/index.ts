/**
 * hello zero-opens
 * 
 * @returns string
 * 
 * @example
 * ```ts
 * import { hello } from 'zero-opens';
 * 
 * console.log(hello());
 * ```
 */
export function hello(): string {
  return `hello zero-opens!`;
}